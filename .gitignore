# Raw data — keep locally, do not commit
*.xlsx
*.xls
*.csv

# Trained model artifacts — artifacts/parcel and artifacts/freight are committed so
# the deployed app has models to load; local backup snapshots stay out of the repo.
artifacts/_backup_*/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
.env

# OS
.DS_Store
Thumbs.db
